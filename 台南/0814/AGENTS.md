# 專案指示

所有回應請使用繁體中文。

本專案是 SwiftUI / iOS 專案。協作時請遵守以下原則：

- 修改程式碼時優先沿用現有 SwiftUI 架構與命名風格。
- Swift 型別使用 PascalCase，屬性與方法使用 camelCase。
- SwiftUI 狀態使用 `@State private var`，常數優先使用 `let`。
- 保持 4 spaces 縮排。
- 優先使用 Swift 的 async/await API，避免新增 Combine。
- 只修改使用者要求範圍內的內容，避免無關重構。
- 回覆說明、程式碼解釋與操作摘要皆使用繁體中文。
